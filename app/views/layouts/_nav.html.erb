<header class="blog-header py-3">
  
  <div class="nav-scroller py-1 mb-2">
  <div class="row">
  <div class="col-sm-2">
  <img src="/TCMOTTC.jpg" alt="TCMOTTC" class="logo" height="75" width="75" />
  </div>
  <div class="col-md-7">
    <nav class="nav">
      
      <%= link_to "Home", root_path, :class =>  "col p-1 text-muted" %>
      <%= link_to "About Us", about_us_path, :class => "col p-1 text-muted" %>
      <%= link_to "Resale", resale_path, :class => "col p-1 text-muted" %>
      <%= link_to "Membership", membership_info_path, :class => "col p-1 text-muted" %>
      <% if user_signed_in? && current_user.profile %>
        <%= link_to "Member Area", users_path, :class => "col p-1 text-muted" %>
        <%= link_to "Profile", user_profile_path(current_user, current_user.profile.id), :class => "col p-1 text-muted" %>
      <% end %>
      
      <% if user_signed_in? && current_user.admin? %>
        <%= link_to "Admin", admin_root_path, :class => "col p-1 text-muted" %>
      <% end %>
      
    </nav>
    <div class="text-center">
    <h3 class="offeringsupport">Offering Support, Education and Friendship to Families with or Expecting Multiples</h3>
    </div>
    </div>
    <div class="col-md-3">
      <div class="login-nav">
        <% if user_signed_in? %>
        <span class="navbar-text">Welcome, <%= current_user.email %>  &nbsp; &nbsp;</span>
        <%= link_to 'Sign Out', destroy_user_session_path, :method => :delete %>
        <% else %>
        <%= link_to "Sign Up", new_user_registration_path, :class => "btn btn-sm btn-outline-secondary" %>&nbsp;&nbsp;
        <%= link_to "Sign In", new_user_session_path, :class => "btn btn-sm btn-outline-secondary" %>


        
        
        <% end %>
      </div>
      </div>
  </div>
  </div>
  <div class="row flex-nowrap justify-content-between align-items-center">
    <div class="col-4 pt-1">
      
    <div class="col-4 ">
      
      <!-- <a class="blog-header-logo text-dark" href="#">Large</a> -->
    </div>
    <div class="col-4 d-flex justify-content-end align-items-center">

       

    </div>
  </div>
</header>


<div class="container">
  <div class="row">
    <div class="col-xs-4 col-md-8 col-lg-12">
      <div class="panel panel-default">
        <div class="panel-heading">
          Committees I am on
        </div>
        <div class="panel-body">
          <% @committees.each do |committee| %>

          <label>Committee Name: </label> <%= committee.name %>

          <%end %>
          <br />
          <br />
          <%= render 'new' %>
        </div>
